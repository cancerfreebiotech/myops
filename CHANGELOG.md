# Changelog

All notable changes to this project will be documented in this file.

## v1.0.9 — 使用者列表看不到人（2026-08-07）

### Fixed（修復）

- **`/admin/users` 使用者列表空白**：Linda 和 Po 用不同帳號、無痕模式都重現同一個問題——
  查到根因是 `is_admin()`、`has_feature()`、`auth_user_role()`、`auth_user_features()`
  這 4 個到處在用的權限判斷函式，本體都會讀 `users` 表自己那一列，但 `users` 表「admin
  可以看全部」的政策條件剛好又引用了 `is_admin()`。平常沒事，因為另一條「使用者是
  active 就能看」的政策會先擋住，不會走到會遞迴的那一條——但這是 2026-08-07 系統上線
  以來第一次有人被停用帳號（Linda 停用了當天早上才建立的一個帳號），`users` 表第一次
  出現 `is_active = false` 的資料，從那一刻起任何掃到那一列的查詢都會遞迴到查詢直接
  爆掉。`/admin/users` 的列表本來就設計成要看到離職/停用的人，必中；程式碼又沒檢查
  資料庫回來的錯誤，直接退化成空陣列——所以畫面看起來乾乾淨淨地「沒有使用者」，
  log 也只有 200，完全看不出是資料庫在噴錯。
  修法：這 4 個函式改成 `SECURITY DEFINER`，讓它們查詢自己身份時跳過 `users`
  的權限檢查，從根本斬斷遞迴（這 4 個函式都只查自己的身份，不能被用來偷看別人資料，
  安全性不受影響）。

## v1.0.8 — 登入偶爾被無故踢出、畫面局部消失（2026-08-07）

### Fixed（修復）

- **併發請求把使用者誤判成登出**：Linda 反映系統變很慢、登入後看不到使用者列表、旁邊
  的功能卡片也不見。查了 production log 發現：她瀏覽器一次載入的頁面（側邊選單＋使用者
  列表）觸發了近 30 個並行請求（Next.js `<Link>` 對可見連結的預先載入），全部剛好撞在
  她的登入 token 快過期、需要刷新的時間點。Supabase 的 refresh token 一次只能被兌換一次，
  同時搶著刷新的請求裡只有一個會成功，其餘的都會拿到「409 併發刷新衝突」——這其中兩個
  請求甚至直接卡住等了 5 分鐘才逾時（這就是「系統很慢」的來源）。
  問題出在 `proxy.ts`：它把這種「只是暫時搶輸」的衝突，和「真的沒登入」一視同仁，一律
  轉址回登入頁；連 API 請求也被轉址（不是回傳 401），前端的 `fetch()` 預設會跟著轉址
  拿到登入頁的 HTML，解析 JSON 失敗，該頁面的內容就悄悄變空白——這就是使用者列表和
  旁邊功能卡片消失的原因。事件在她重新整理成功登入後（05:04 左右）已自行恢復，
  但同樣的併發搶跑隨時可能在任何人身上重演。
  修法：這種暫時性的 409 衝突不再轉址（cookie 裡的 token 其實還沒真的過期，讓請求
  正常往下走，資料存取仍受資料庫 RLS 保護）；真的沒登入的 API 請求則從「轉址」
  改回「回傳 401 JSON」，避免前端 fetch 把登入頁的 HTML 誤當成資料解析。

## v1.0.6 — storage 下載漏洞補完、使用者文件全面重寫（2026-08-06）

### Security（安全性）

- **檔案下載的 IDOR 補完**：2026-07-23（v0.9.0）修過一次「任何登入者知道檔案路徑即可下載任意附件」，
  但那次只補了 `/api/storage/download` 這個轉發端點（加上物件層授權檢查），沒有動到
  Supabase Storage 本身的存取政策。而瀏覽器本來就握著能直接呼叫 Storage API 的金鑰，
  任何登入者其實可以完全跳過那個轉發端點，直接對 Storage 服務下 `list()`／`download()`，
  等於漏洞只塞住了一半。這次把 `documents`、`feedback-screenshots`、`expense-receipts`、
  `leave-files`、`training-files` 五個儲存空間的存取政策直接改成「只有本人／HR／管理員
  依各自資料表既有的規則才能讀」，從根本堵住，不再依賴前端有沒有經過轉發端點。
  （第六個空間 `asset-files` 本來就設計成全員可讀，不算漏洞，沒有改動。）
  已實際查過修復前的曝光量：問題回饋截圖 35 個檔案、一般文件 3 個、報帳單據 2 個，
  在修復前任何登入者皆可讀取；特殊假附件與教育訓練檔案當時尚無人上傳，漏洞存在但未造成曝光。

### Docs（使用者文件）

- **`docs/generated/` 六份使用手冊全面重寫**：原文件停在 2026-06-11（v0.3.2），這段期間
  上線的採購全流程、薪資四關簽核、績效考核、教育訓練、資產管理、招募、入離職、Teams
  一鍵簽核等大量功能完全沒有反映進文件。這次逐模組重新查證現在的實際行為（不是憑印象改寫），
  三語（中/英/日）× 兩種深度（一般使用者／管理員，各自涵蓋 78 個子模組的完整盤點）全部重寫，
  並依規範補上流程圖與常見問答。
- **應用程式內建的 `/help` 說明頁同步更新**：原內容停在 2026-07-11（v0.8.x），這次三語區塊
  都補上遺漏的模組卡片（薪資級距管理、採購全流程、招募、入離職、系統設定等）、更正過期或
  不準確的描述，並擴充權限對照表；三語卡片數與矩陣列數已核對一致。
- 過程中順帶記錄了幾個目前程式碼裡真實存在、但文件容易寫錯的行為落差（例如：打卡頁認
  `job_role` 判斷 HR、但請假/加班/出差/行事曆四頁只認 `granted_features`，同一個人在
  不同頁面可能被判定成不同身分；加班待審清單目前後端一律回傳全公司待審單，前端才依身分
  決定要不要顯示，所以一般員工看得到別人的加班單卡片是正常現象，不代表能核准），
  文件裡已照實寫清楚而非美化成統一規則。

## v1.0.5 — Teams 簽核卡片按鈕可以用了（2026-07-31）

### Fixed（修復）

- **Teams 簽核卡片的「核准／退回」按鈕**：Dr.Ave 那邊修好卡片渲染之後（原本所有帶按鈕的
  通知從 7/29 起就沒有送達過任何人），按鈕按下去仍然會失敗——因為回呼的欄位形狀與
  myOPS 這端讀的不一樣（對方送 `{ actionType, payload, actor }`，我們照舊規格讀扁平的
  `{ email, docType, ... }`）。現在兩種形狀都收，卡片按鈕可以正常簽核。
  - 點擊者身分只認 Dr.Ave 認證過的 `actor.email`，不從卡片自帶的 payload 取。
  - 非簽核用的按鈕（例如「前往簽核」的連結）會明確回報，不再誤報成「缺少必填欄位」。

### Notes（技術）

- 上線後以 6 種 body 形狀對正式站實測（巢狀／扁平／缺 actor／open_url／錯 token），
  全部行為正確；測試一律用不存在的單號，不會動到真實單據。
- 對方已知個案：有一位同事在 Teams 封鎖了 Dr.Ave，他若是簽核人，通知會失敗並在
  log 顯示 `method:"failed"`——那需要本人在 Teams 解除封鎖，不是系統問題。

## v1.0.4 — 稅額可覆寫、請款防重、每日回饋彙整補上留言（2026-07-31）

### Added（新增）

- **稅額改為「自動算出、可手動調整」**（Linda 7/30 補充說明、Scott 回報 22dafc0d）：
  - **請購單**：稅額原本是唯讀的、只能改稅率。現在稅額本身可以直接填。
  - **驗收單**：稅額原本可以填、但完全不會自動算。現在改小計或稅率就會自動帶入稅額，
    也仍然可以自己覆寫。
  - 兩張單都一樣：手動填過的稅額會被記住，之後改稅率**不會**再把它蓋回自動值；
    想回到自動計算，按稅額欄位下方的「恢復自動計算」。
  - 合計一律是「小計＋稅額＋運費」，所以覆寫稅額不會讓同一張單上的三個數字互相矛盾。
    驗收單的合計因此改為顯示計算結果（小計沒填的舊單不受影響，仍可自行填合計）。
  - 說明一下範圍：請購單的金額是**伺服器端**重算的，前端改不動；驗收單的合計目前只由
    表單維持一致，API 仍接受任意數字（要一併改動 270 張既有驗收單，其中含小計為空的
    Ragic 舊單，所以留待後續）。

### Fixed（修復）

- **同一張請購單不會再被請款兩次**：先前一張請購單可以開出多張進貨驗收單，
  而每張驗收單各自都能請款一次 —— 全額重複付款的路徑是通的（7/31 就出現過兩張同額
  的驗收單）。現在改成**一張請購單只能有一張未作廢的採購請款單**，資料庫層也上了同樣的限制。
  要分批付款請在該請款單上用「建立分期請款」；若原請款單有誤，作廢後可以重轉。
  重複轉單時的錯誤訊息也從籠統的「不支援此轉單組合」改成講清楚原因與下一步。
- **每日回饋彙整信看不到留言**（內部工具）：彙整只查了新單，使用者在既有單上追加的說明
  與回覆完全不會出現；另外「今天」的起算點少算了 8 小時，前一晚的回報會被算成今天的。
  兩者都已修，並補上 `--dry-run` 與 `--since=YYYY-MM-DD`（可補寄漏掉的某一天）。
- **Teams 通知失敗的紀錄改為記下實際回應內容**：原本只留一句「not-ok」，
  分不出「對方明確拒絕」與「回應格式不對」。

### Notes（給 Linda）

- **每日填報的「今日行程／完成回報 重複填寫」（00dbbd55）其實 7/22 就做好了**，
  只是當時的更新說明沒有寫出來，所以你不知道。做法是：**今日行程每一列左邊都有一個圈圈，
  點一下變成綠色勾勾就是完成**，按「儲存」後會自動同步到「完成回報」分頁，不必再重打一次
  （完成回報那邊標示「行程」的項目就是從今日行程來的，可以在那裡補備註）。
  另外「早上行程」也已經改名為「今日行程」。請你確認一下這樣是不是你要的；
  如果操作上不好找，我們可以把那個圈圈改成有文字的勾選框。
- 勞健保級距表**還是只有 58 級**，部分工時那 17 級仍然沒有進系統。程式已經修好了，
  但要**麻煩你把同一個檔案再上傳一次**才會補齊。
- 7 月那 7 筆薪資草稿目前仍是舊的勞退提繳基礎（雇主提繳偏低），**建議先按一次「批次計算」
  重算再開始簽核**；重算會把單子退回草稿並清掉已完成的簽核，所以要在簽核開始前做。

### Notes（技術）

- migration `20260731000001`（稅額手動旗標、請款單帶入請購單、請購單層級請款唯一索引）
  已套用正式庫；套用前以交易回滾方式驗證 6 項斷言（含「作廢後可重轉」與「歷史單不受限」）。

## v1.0.3 — 薪資明細頁、勞退分級表、整批簽核（2026-07-30）

本段涵蓋 v1.0.0～v1.0.3。

### Added（新增）

- **薪資表點員工可以看「薪資明細」**（回報 `76821eb3`，依 Linda 7/29 指定的順序）：人資從薪資表點員工姓名，就會進到該期薪資的組成頁——應發項目（本薪、加班費、獎金、其他加項）、應扣項目（無薪假扣款、勞保費、健保費、勞退自願提繳、其他扣項）、實發金額、雇主負擔（勞保、健保、勞退 6%）、簽核軌跡（誰在什麼時候審核／確認／核准／發放／退回）、異常提示與備註。員工在「我的薪資單」也可以點進自己的明細。
  **設計方向與原因**：這一頁刻意**只呈現目前已經存在的欄位、不新增任何計算**。Linda 指定的順序是「先能看組成 → 再把底薪／加班費／扣除項拆成逐筆明細 → 最後才依勞基法與公司規則調整計算公式與薪資單格式」，先看得到才知道要拆什麼、公式該怎麼改。
  **顯示「—」與「NT$ 0」的差別是刻意的**：「—」代表這一項**沒有算**（手動建立的薪資單不含保費欄位），「NT$ 0」代表**算出來是零**（級距表還沒上傳時保費真的會算成 0）。兩者混成同一種顯示會把「級距表沒上傳」這個問題藏起來。
  **權限**：先查資料再判斷權限，查不到就一律視為沒有權限。原因是資料庫的可見範圍與程式的權限旗標並非完全一致（資料庫認的是人資／財務／營運長／管理員或本人），先用旗標放行再假設查得到資料，兩邊一旦分歧就會出現「有權限卻整頁空白」。員工看自己的薪資單仍限於「營運長已核准」與「已發放」，直接輸入網址也不會看到未完成的薪資。
  **下一步**：把三大類金額拆成逐筆明細（例如加班費列出每一筆加班、扣除項列出每一項扣款）。

### 回覆 Linda 的問題（勞健保級距上傳，回報 `774a2ea3`）

- **「部分工時」那幾級被系統丟掉——已修**：您上傳的檔案在等級 1 之前有 17 列「部分工時」（投保薪資 11,100～28,590），但等級欄位不是數字，系統的匯入程式把它們當成無效資料**靜默略過**，只收了編號 1 以後的 58 級（從 29,500 起），而且沒有告訴您略過了幾列。現在改成只看「投保薪資」判斷是不是有效的級距列，部分工時的級別文字照原樣保留顯示；**任何被略過的列都會顯示筆數與原因**。
  **⚠️ 需要您再上傳一次**：程式修好了，但資料庫裡目前仍只有 58 級——同一個檔案再上傳一次，那 17 級就會補齊。
  **目前沒有影響任何人的薪資**：現職 8 位同事都是全職，批次計算也只納入全職，所以不會有人被套到錯的級距。但若之後有部分工時同事，投保級距會抓到最低的 29,500、保費算多。
- **不需要另外上傳「薪資對級距」的對照表**：系統就是拿實際薪資去比您上傳的「投保薪資」欄位，取**第一個大於或等於該薪資的級距**（超過最高級距就用最高級）。所以只要級距表本身完整（含部分工時那幾級），對照就會自動成立。
- **勞退分級表已補上（含 2026 年度資料，不必您上傳）**：原本勞退提繳是直接用「月薪 × 6%」計算，沒有走法定的「勞工退休金月提繳工資分級表」——例如實際月薪 30,000 的月提繳工資應為 30,300，直接用月薪算會與申報值不符。現在已改為依分級表計算，並已預先載入 2026 年度的 62 級。
  **資料來源**：勞動部勞工保險局「勞工退休金月提繳分級表」（中華民國 114 年 11 月 24 日勞動部勞動福 3 字第 1140153598 號令修正發布，自 115 年 1 月 1 日生效）。數字是由官方 PDF 直接解析、沒有人工輸入，並經程式檢查級別連續、金額遞增、每級與區間上限一致、區間首尾相接。
  **勞健保級距頁面下方新增「勞退 — 月提繳工資分級表」區塊**，可查看目前級距，未來年度也可自行上傳（同樣有範本下載）。個人自願提繳率仍在每位同事的人事資料裡設定。
- **拜託幫我們核對一次**：級距金額我們不會自己填（填錯一格就是全員保費算錯）。目前載入的是 2026 年勞保、健保各 58 級。我們檢查過內部一致性（勞保個人負擔在 1,145 封頂，回推投保上限為 45,800；健保個人負擔＝投保金額 × 5.17% × 30%），但**沒有逐格對照官方公告**。正式發薪前，麻煩您或會計對一次筆數與幾個關鍵級距。

### Changed（薪資簽核流程，回報 `26011df2`）

- **整批簽核：每一關按一次就好**：薪資表上方新增「整批簽核」列，每一關一顆按鈕並顯示該關有幾筆待簽（例如「HR 審核（4 筆）」），按一次就把該關所有薪資單一起推進下一關，不必每位同事各按一次。部分失敗會指名是哪幾筆沒過。
  **設計方向與原因**：刻意做成「**同一關的多筆一起簽**」，而不是「一個按鍵從草稿直接跑到已發薪」。四道關卡的意義就是分段覆核，一鍵跑完等於沒有簽核，而薪資是最需要責任歸屬的資料。整批簽核在底層仍是逐筆走原本的簽核程序，所以 MFA 驗證、逐關權限、狀態檢查與防重複寫入的保護全部保留。
- **第三關由「營運長核准」改為「人資長核准」**：人資長為 Eva Hung，已設定對應權限；營運長不再參與薪資簽核。流程現為 HR 審核 → 財務確認 → 人資長核准 → 確認發薪。
  **設計方向與原因**：只調整權限與顯示文字，資料庫內部的狀態代碼沿用舊名——改代碼名稱要動狀態約束與兩個欄位，對正在流程中的薪資單沒有任何好處。另外「加班」模組的第三關**仍然是營運長**，兩者共用同一組狀態文字，所以只在薪資頁面覆寫顯示，不動共用文案。

### 需要注意（給人資）

- **7 筆薪資草稿建議重新計算一次**：勞退提繳的計算基礎從「月薪」改為「月提繳工資」之後，7/30 之前產生的草稿裡的雇主勞退提繳金額是舊算法（金額偏低），重新按一次「批次計算」就會更新。
  **但請注意**：重新計算會覆寫當月既有薪資單並**退回草稿、清掉已完成的簽核**。若已經推進到 HR 已審／財務確認等階段，重算會把它們打回原點。建議在還沒開始簽核前先重算一次，之後再一路簽下去。

### Internal（內部，不影響使用者操作）

- **版號進位規則修正**：原本只有第三碼滿 9 才進位，所以 0.9.9 的下一版變成 0.10.0。已改為每一碼上限都是 9（第三碼滿 9 進第二碼、第二碼滿 9 進第一碼），因此這一版收斂為 **1.0.x**。已發布的 0.10.x 不回頭修改。

## v0.10.2 — 7/29 回報修復（薪資批次計算、採購金額門檻、報帳表單、單據上傳）（2026-07-29）

本段涵蓋 v0.10.0～v0.10.2（同一批修復分三次部署，中間兩版沒有獨立的使用者可見變更）。版號從 0.9.9 跳到 0.10.x 是自動化流程的進位規則所致；規則已修正為第二碼也上限 9，**下一版起會進位到 1.0.0**。

以下說明**改了什麼、為什麼這樣改**。文末有幾項需要 Linda、Scott 提供資訊才能繼續，麻煩看一下。

### Added（新增）

- **薪資可以一次計算全部員工**（回報 `22ffe728`）：薪資頁多了「批次計算」按鈕，會依人事與出勤資料，為所有在職／台灣／全職且已填月薪的同事一次產生薪資草稿（計算來源：月薪、已核准加班、無薪假扣款、獎金紀錄、勞健保級距表）。
  **設計方向與原因**：這個計算功能其實一直存在，只是**沒有任何按鈕連到它**，也沒有排程呼叫，所以從外面看就像「只能一筆一筆建立」。這次不是新寫演算法，而是把既有計算接上畫面，並在按下去之前先把「會影響哪些資料」講清楚：本月已進入簽核的薪資單有幾筆、重算會把它們壓回草稿並清掉已完成的簽核（此行為是否應該改成「跳過非草稿」仍待拍板，見下方待確認事項）。
  **順帶補上兩個沉默的失敗**：（1）原本寫入失敗會被程式忽略，只顯示「產生 0 筆」，看不出是沒有符合條件的人、還是寫入被擋——現在會把失敗原因顯示出來。（2）人事資料沒填月薪的同事會被跳過，但原本的訊息只算「真的建出幾筆」，所以會顯示成「已產生 7 / 7 筆」看起來像全部跑完。現在會明確顯示「跳過 N 人（人事資料未填月薪）」。**目前有 1 位同事尚未填月薪**，批次計算會跳過他，補上人事資料後重算即可。

- **請採購單依金額決定簽核關卡**（回報 `acefefa5`）：3,000 元以下只需部門主管；超過 3,000 元加營運長；超過 20,000 元再加執行長。三段都保留最後的「通知採購」關卡。門檻金額可在「營運長設定」頁自行調整，不需要改程式。目前 255 張歷史請採購單裡有 96 張（38%）在 3,000 元以下，過去一律走四關。
  **設計方向與原因**：門檻沒有寫死在程式裡，因為這是會隨公司規模調整的營運政策，寫死就代表每次調整都要重新部署。另外，關卡名稱以前不存在資料庫裡——畫面是用「第幾關」去固定清單取第 N 個名稱。關卡數一旦隨金額變動，小額單的第 2 關（通知採購）就會被標成「COO」。所以送簽時會把當下決定的關卡名稱一起寫入，畫面直接讀它，不再用位置推算。已在簽核中的單不受影響（沿用原本的顯示方式）。
  **另外**：草稿階段會顯示「依目前金額，送簽後將經過：⋯」，送簽前就知道要跑幾關。

- **報帳表單改為會計科目，並加上申請月份與發票號碼**（回報 `87f7663a`）：類別改為差旅費／交際費／職工福利／文具印刷／雜支／其他費用；新增「申請月份」（預設當月）與「發票憑證」欄位。匯出的報帳明細也加入這兩欄。
  **設計方向與原因**：舊類別（交通、誤餐、用品）是「使用者角度的用途」，記帳時還要人腦轉一次科目；直接用科目命名，報帳單就能拿去對帳。申請月份刻意與費用日期分開，因為 7/31 的收據可能算 8 月報帳——月結看的是申請月份，所以**匯出的月份篩選也改以申請月份為準**。既有資料只有 1 筆，已明確轉換而非重建。

### Fixed（修復）

- **發票單據與出貨單據可以上傳檔案了**（回報 `4a05ca46`）：進貨驗收單的這兩個欄位原本是純文字輸入框（只能貼網址），現在是真的上傳檔案，上傳後可直接檢視。
  **設計方向與原因**：檔案存在系統自己的儲存空間、以物件路徑記錄，讀取一律經過下載端點換取短效簽章網址，才吃得到既有的逐單授權（看不到那張單的人也拿不到檔案）。上傳後仍需按「儲存」才會寫入單據，與其他欄位一致，避免以為上傳就等於存檔。
  **同時發現一件更大的事**：從 Ragic 搬過來的舊單據附件**只搬了檔名、檔案本身還在 Ragic**，全系統共 584 筆（進貨驗收單的發票與出貨單據最多，另有商品圖片、廠商存摺與發票章、請款發票等）。這些欄位以前會產生一個點不開的死連結，現在改為顯示原始檔名並標註「附件仍在舊系統」，不再假裝檔案在這裡。要不要真的搬過來，需要決定（見文末）。

- **下拉選單不再顯示一長串編號**：選完員工／假別／專案／廠商之後，欄位顯示的是內部編號（UUID）而不是名稱。
  **設計方向與原因**：這是元件層的問題——我們用的下拉元件在沒有給「編號→名稱」對照表時，會直接把編號印出來。全站共 28 處下拉都中此問題（Linda 的截圖是薪資的員工欄位）。修法是在共用元件裡自動從選項推導對照表，而不是去 28 個地方各補一份對照表：後者容易改了選項忘了改對照表，而且以後每加一個下拉都要記得補。

- **上傳勞健保級距表的錯誤訊息指錯方向**（回報 `b1203e93`）：上傳官方 PDF 時，畫面顯示的是「找不到必要欄位」，讓人以為欄位名稱不對，其實是檔案格式不支援（系統只讀 Excel／CSV，而拖放進來的檔案不受「選擇檔案」對話框的格式限制）。現在會直接說明是格式問題，並提供**空白範本下載**與必要欄位說明。

### 需要 Linda 提供資訊才能繼續

1. **薪資單內容**（回報 `76821eb3`）：您給的參考格式包含「應稅金額」與「年度應稅總計」，這兩項需要先定義清楚才能算：
   - 哪些項目免稅、上限多少（例如伙食津貼每月免稅額）？
   - 「應稅金額」是指當月應發扣除免稅項目後的金額嗎？「年度應稅總計」從哪個月起算（1 月／到職月）？
   - 參考格式裡的「工號」目前系統沒有這個欄位——編碼規則是什麼（例如 E0072 的規則）、由誰維護？
   - 「匯入帳號」目前 8 位同事的人事資料裡**都還沒有填**，需要人資補齊才顯示得出來。
   - 另外，勞健保與勞退的計算基礎（底薪 vs 月薪資總額）、眷屬健保費是否代扣，這兩項仍待人資確認，會影響薪資單的金額。
2. **勞健保級距表資料**（回報 `b1203e93`）：其實最好的做法是我們直接把 2026 年度官方級距預載進系統，您就不需要每年上傳。但級距數字不能由我們憑印象填（錯一格就是全員保費算錯），麻煩提供官方級距表的 Excel／CSV，或指定要以哪份公告為準，我們載入後會核對筆數與金額。**在級距表載入前，批次計算出來的勞保費與健保費會是 0**，畫面上也會提醒。
3. **舊系統（Ragic）的 584 筆附件**：要不要把檔案真的搬進 myOPS？若要，需要 Ragic 的匯出檔或 API 權限；若不搬，就維持現在「顯示檔名並標註仍在舊系統」的做法。
4. （給 Scott）**採購單稅額**（回報 `22dafc0d`）：目前「稅率(%)」可以填，稅額是自動算出來的（小計×稅率）。您需要的是哪一種——**直接填稅額**（可覆寫自動計算，適合發票稅額有進位差異時），還是**選稅別**（應稅／零稅率／免稅，選了自動帶 5%）？告訴我們就做。

### 待拍板（沿用上一版，尚未決定）

- 重跑當月薪資是否可覆寫已核准／已發放的紀錄（目前會壓回草稿並清除簽核軌跡，按下前會警告）。
- 勞健保／勞退計算基礎、眷屬健保費代扣。
- 設計系統的視覺決策（品牌主色、標題字型、公告分類配色、打卡鈕尺寸）。

## v0.9.9 — 安全性強化、行事曆隱私、庫存與薪資修復（2026-07-29）

這一版沒有使用者回報的項目，全部是我們主動稽核後修的。以下說明**改了什麼、為什麼這樣改**。

### Security（安全性）

- **修復授權判斷在「未登入」情況下失效的問題**：資料庫用來判斷「這個人是不是管理員／有沒有某項權限」的兩支判斷式，在查不到使用者時回傳的是「未知」而不是「否」。程式碼寫成「**不是**管理員就拒絕」時，「不是未知」的結果仍然是未知，而資料庫遇到未知條件不會進入拒絕分支——等於那道門形同不存在。實測確認：任何人不需要登入，就能讀到全公司已核准的請假（姓名＋假別＋日期）以及採購驗收的廠商名稱與金額。
  **設計方向與原因**：我們沒有逐支函式去補檢查，而是直接把那兩支判斷式改成「查不到就回否」。原因是投入最小、覆蓋最廣——一次修好就讓所有依賴它的閘門同時變成「預設拒絕」，包含「員工用自己的申請單自行核准並核給假別天數」這條繞道；逐支補檢查則永遠會漏掉下一支。同時發現另一個同類邊界：使用者的「特殊權限」欄位為空時也會產生同樣的未知結果，已一併處理。
  **第二層防線**：另外把所有「未登入即可呼叫」的資料庫函式權限全部收回（收完後為 0 支），登入者的權限完全不變。這樣即使日後有人再寫出同類的錯誤判斷，未登入者也連函式都執行不到。
  **影響期間**：2026-07-06 至 2026-07-29，約三週。目前沒有跡象顯示曾被外部利用；此類讀取不會留下應用層紀錄，故無法完全排除。
  **順帶修好**：自動單號產生函式原本也對外開放（可被任意呼叫造成單號跳號、稽核序號不連續），已收斂為僅系統後端可用。

### Changed（調整）

- **公司行事曆只顯示「請假中」，不再顯示假別**：原本行事曆會對全體同仁顯示「某某某 生理假／病假」。
  **設計方向與原因**：同事之間為了安排工作需要知道「某人今天不在」，但不需要知道「為什麼不在」——生理假、病假屬於健康個資。因此改為只顯示姓名與「請假中」，資料庫層也不再回傳假別欄位（不是只把畫面藏起來）。
  **刻意保留**：請假頁的「請假行事曆」仍會顯示假別與事由，因為它的可見範圍是同部門與人資，是主管排班協調用的功能，收掉會讓主管無法運作。若這裡也要收斂，屬政策決定、需另行討論。

### Fixed（修復）

- **薪資「確認發放」從上線以來一直失敗**：程式在確認發放時要寫入「發放時間」，但這個欄位從來沒有被建立過，所以每次都被資料庫擋下、薪資單永遠停在「COO 已核准」。已補上欄位，發放流程可以走完。
- **薪資簽核現在會留下軌跡**：人資審核、財務確認、COO 核准、確認發放、以及退回，各自記錄「是誰、什麼時候」做的。
  **設計方向與原因**：欄位本來就存在，只是程式一直只改狀態沒寫人。薪資是最需要責任歸屬的資料，反而沒有軌跡。這次只補「資料要記錄」，畫面顯示留待真的需要查核時再做，避免為了加一欄顯示而改動表格與三語文案。
- **保險級距上傳改為「全有或全無」**：原本是「先寫入新的一整年、再刪掉舊的」兩個步驟，中途失敗（或執行逾時）就會同時留下新舊兩套級距，之後算薪資會抓到錯的那筆，每張薪資單的勞健保都算錯且不會報錯。現在改成在同一筆交易內完成，失敗就整批不生效。
- **收貨單重複轉入庫不會再重複計庫存**：同一張進貨驗收單原本可以被重複轉成多張入庫單，全部過帳後庫存被加了兩次。
  **設計方向與原因**：沒有用「一張驗收單只能轉一張入庫單」的限制，因為分批收貨、分批入庫是正常作業，那樣會把正常流程擋死。改成**擋數量、不擋張數**：過帳時回頭累加同一張驗收單底下所有已過帳的入庫量，超過驗收數量就擋下並指名是哪個商品、還剩多少可入庫。真正的分批入庫照樣可以過帳；取消過帳或作廢會自動把額度退回。經查正式庫，目前尚無任何經此路徑過帳的入庫單，因此沒有既存的錯誤庫存需要修正。

### Internal（內部，不影響使用者操作）

- **文件問答的可用文件範圍收斂為單一設定來源**：原本「哪些文件可以被問答引用」寫死在三個地方（目前內容一致，不是現行漏洞）。任一處日後被改動而其他兩處沒跟上，語料範圍就會不一致，最壞情況是合約、NDA 這類機密文件被餵進對全體員工開放的問答。已收斂為單一來源，未來要新增文件類型只會有一個地方要改。檢索邏輯與結果完全沒有變動。
- **設計系統文件（MASTER.md）全面校正**：文件記載的技術棧與實際落差大到會「教錯人」（例如文件寫主色是藍色，實際共用元件的主色接近黑色）。已逐節對照程式碼修正，並新增維護規則說明。同時記錄了幾個已確認但未修的技術債（標題字型設定失效、兩套色彩系統並存），修它們會改變全站視覺，需要先確認。

### 需要拍板的事項（本次刻意未動）

- **勞健保與勞退的計算基礎**：目前只以底薪計算，未含加班費與獎金；勞保條例與勞退條例的申報基礎是「月薪資總額」。眷屬健保費目前完全沒有從薪資扣除。這三項都需要人資確認正確做法後才動公式。
- **重跑當月薪資是否可覆寫已核准／已發放的紀錄**：目前會一律壓回草稿，需決定是要跳過非草稿、還是要求先退回才能重算。
- **設計系統的幾個視覺決策**：品牌主色是否統一、卡片與對話框標題字型、公告分類是否恢復分色、打卡按鈕尺寸。

## v0.9.8 — 已開始的假只能由人資撤銷（2026-07-28）

### Changed（調整）
- **已經開始（含今天）的請假只有人資／管理員可以撤銷**：假已經在休、出勤與額度都已受影響，不開放當事人事後自行把額度收回。員工端不會顯示撤銷按鈕，需要撤銷請洽人資協助。**尚未開始**的假，申請人仍可自行撤銷。

## v0.9.7 — 7/28 使用者回報修復（撤銷已核准請假、全員紀錄分頁統一）（2026-07-28）

### Added（新增）
- **可以撤銷已核准的請假**（回報 `1a3f5591`）：先前只有「待審核」的請假可以取消，已核准的單子員工和管理員都動不了。現在已核准的請假可以撤銷，撤銷時會自動退還當初扣掉的假別額度、並移除已同步到 Outlook 的行事曆事件；人資代為撤銷時會用 Teams 通知當事人。撤銷會留下操作人、時間與原因的紀錄可供查核。

### Changed（調整）
- **請假／加班／出差／打卡的「全員紀錄」分頁格式統一**（回報 `d21a92f5`、`572ea8fb`）：四個模組的全員紀錄現在都是同一套版面——月份、員工、狀態篩選加上分頁，不再是一張看不完的長表格。
- **打卡紀錄管理併入打卡頁面**（回報 `d21a92f5`）：原本「打卡」頁的「團隊總覽」和側邊欄的「打卡紀錄管理」內容重複，現在整合成打卡頁的「全員紀錄」分頁（含統計、篩選、分頁與作廢功能），側邊欄不再有重複入口。舊的「打卡紀錄管理」連結會自動轉到新位置。
- **假別額度只留一個入口**（回報 `1a3c6d9c`）：「HR 管理」頁裡的假別額度表格移除，統一由側邊欄的「假別額度」頁面維護，避免同一份資料兩個地方各自維護、數字不一致。
- **使用者管理頁加上帳號開立說明**（回報 `d2c28e89`）：頁面上直接說明——**使用者帳號一律由 IT 部門統一開立，人資無法在此新增使用者**。同仁首次登入後帳號會自動出現在清單中，**預設權限為「一般員工」**，部門、主管與特殊權限再由人資在此頁調整。

### Fixed（修復）
- **請假額度的權限漏洞**：請假額度的加減是由資料庫函式處理，原本設定成任何已登入的使用者都可以直接呼叫，等於有機會自行把已用天數歸零、取得無上限的假。已收斂為只有系統後端可呼叫。此問題自額度功能上線起就存在，目前未發現被利用的跡象。

### 說明（非本次變更，回覆使用者疑問）
- **事假、病假不需要人資設定額度**（回報 `2ebb523f`）：事假 14 天（無薪）、病假 30 天（半薪）、生理假每月 1 天等法定天數，系統的假別設定裡**早就已經內建**，人資不需要逐人設定，員工現在也已經可以直接申請、不會被卡住。假別餘額頁在人資尚未另外核給額度時會顯示「法定額度（人資尚未設定）」，那句提示只是用來區分「系統帶的法定值」與「人資另外核給的額度」（例如特休調整、需審核資格的特殊假），並不代表還有待辦事項。人資真正需要設定額度的只有特休的個別調整與需審核資格的特殊假。

## v0.9.6 — 詢價比價結果自動帶入請採購單（2026-07-28）

### Added（新增）
- **轉採購單自動帶入品項與比價結果**：詢價單簽核完成後按「轉採購單」，新的請採購單會直接帶出詢價單的所有請購品項，單價自動取自各品項勾選「採用」的那筆報價，並一併算好小計與總金額，不必再重新打一次品項和價格。
  - 尚未比到價的品項也會帶入，只是單價留空，方便採購自行補上。
  - 廠商資料：當所有採用報價都來自同一家廠商時，自動帶入該廠商（含統編、聯絡人、電話、Email、帳單地址、付款方式與條件）；若採用了多家廠商的報價，代表需要拆成多張採購單，廠商欄位會留白由採購決定，不會亂帶其中一家。
  - 稅率沿用請採購單原本的設定方式（預設未設稅率），採購設定稅率存檔後金額會自動重算。

## v0.9.5 — 詢價報價「採用」唯一性由資料庫保證（2026-07-28）

### Fixed（修復）
- **一個品項只能採用一筆報價**：原本只有畫面與後端程式在把關，改由資料庫加上唯一性限制，即使並發操作或日後由其他管道寫入資料，也不會出現同一品項有兩筆「採用」報價、進而在簽核後重複建立廠商商品價格。未採用的報價不受影響，同一品項仍可保留多家比價。

## v0.9.4 — 詢價單廠商下拉選單與採用報價自動建檔（2026-07-28）

### Added（新增）
- **採用報價自動寫入廠商商品價格**：詢價單簽核完成後，系統會把每個品項勾選「採用」的那筆報價自動登錄到廠商商品價格清冊（含廠商、單價、報價日期，並註記來源詢價單號），不必再手動重打一次；同一張詢價單重複觸發不會重複建檔。
- **廠商改為下拉選單**：品項的「建議廠商」與報價的「廠商」改從既有的廠商清冊（採購 → 廠商）選取，不再需要手打廠商名稱，避免同一家廠商名稱不一致。廠商本身仍在原本的廠商管理頁維護。

## v0.9.3 — 詢價單可填請購商品與廠商報價（2026-07-28）

### Added（新增）
- **詢價單「請購商品」明細**：詢價單終於可以直接開列請購品項（項次／商品名稱／商品編號／規格／單位／需求數量／用途說明），不必再等轉成採購單才看得到內容。
- **逐項廠商報價比價**：每個品項都可登錄多家廠商的報價（廠商／報價單價／報價日期／備註），並勾選「採用」標記最終選定的那一家，方便比價與後續轉單。
- **廠商報價單附件**：整張詢價單可上傳多份廠商報價單檔案，並在明細頁直接下載。

### Changed（調整）
- 詢價單明細頁重新分區：上半部是「本詢價單自己的」請購商品與詢價結果（可編輯），下半部維持「相關採購單」帶出的唯讀資料，兩者標題已加註區隔避免混淆。
- 簽核中的欄位鎖定延伸到品項、報價與報價單附件：簽核期間僅詢價人員可修改，與既有表頭欄位規則一致。

## v0.9.2 — 7/27 使用者回報修復（4 項）（2026-07-27）

### Added（新增）
- **人資可查看全員紀錄**：請假、加班、出差三個頁面各新增「全員紀錄」分頁（人資／管理員可見，唯讀），可查看所有同仁的完整紀錄；先前只有打卡有全員檢視。
- **假別額度管理入口**：側邊欄新增「假別額度」，不必再從人資設定裡找。
- **庫存單據獨立頁面**：入庫單／出庫單改為點選後進入完整頁面（與請採購單一致），不再是無法調整大小的小彈窗；可直接分享單據連結。

### Fixed（修復）
- **假別額度顯示 0**：人資設定頁的假別額度表格所有欄位都顯示 0，已修正為正確顯示當期額度（含 8–12 月到職同仁的週年制特休）與已使用天數；事假、病假等固定額度假別會顯示法定天數。
- **員工端假別餘額**：事假、病假原顯示「尚未設定額度」，改為顯示法定額度並標註「人資尚未設定」。
- **加班頁人資權限失效**：加班模組判斷人資身分的條件寫錯（比對不存在的角色值），導致人資看不到應有的資料，已對齊全站規則。
- **詢價單簽核卡關**：詢價單新增「部門主管」簽核關卡；並修正「請購人同時是詢價人」時單據無人可簽、只能由管理員強制通過的問題。

## v0.9.1 — 修復意見回饋頁面無法開啟（2026-07-27）

### Fixed（修復）
- **意見回饋頁面 500**：`/feedback` 清單頁在伺服器端呼叫按鈕樣式函式 `buttonVariants()`，但該函式綁在 client 元件模組上，Next.js 不允許從 server 呼叫，導致整頁伺服器錯誤、使用者無法進入或送出回饋。已將 `buttonVariants` 抽離為獨立的非 client 模組（server/client 皆可使用）。

## v0.9.0 — 使用者回報修復＋安全性強化（2026-07-23）

### Added（新增）
- **特殊假別線上申請**：婚假／喪假／產假／產檢假／陪產檢假／陪產假／安胎假／育嬰留職停薪等特殊假別，員工可在「請假 → 特殊假申請」分頁線上提出（選假別＋填原因＋上傳多個證明文件），HR 於同分頁審核並核給天數，核准後系統自動開放該假別申請；申請送出與審核結果皆以 Teams 通知（migration 20260723000003）
- **打卡紀錄管理入口**：管理端打卡紀錄「作廢」功能補上側邊欄與手機導覽入口（功能原已存在，先前僅缺入口而找不到）
- **歷史詢價單明細補全**：詢價單詳情頁新增「品項與數量」「廠商與報價結果」「相關採購單」三個區塊，顯示由 Ragic 轉入、連結於下游採購單的完整資料

### Fixed（修復）
- **假別餘額頁顯示 0**：週年制特休（8–12 月到職者）與固定額度假別在餘額管理頁一律顯示 0，改為依「當期」正確解析餘額，並顯示應有額度與已用天數
- **請假餘額並發超扣**：同一餘額列的多張假單同時核准時可能重複扣減，改為資料庫原子扣減（migration 20260723000002）

### Security（安全）
- **檔案下載授權強化**：`/api/storage/download` 先前任何登入者只要知道檔案路徑即可下載任意附件（IDOR），改為需具備存取「引用該檔案之資料列」的權限才發放下載連結

### Notes
- 移除每頁「測試中」標記
- migration 20260723000002（請假餘額原子扣減）、20260723000003（特殊假別資格申請）已套用正式庫

## v0.8.6 — 修復長期靜默失敗的查詢與採購編號（2026-07-20）

### Fixed
- **採購單建立修復**：新增採購文件時的自動編號（`PR-YYMM-NNN`）先前會被資料庫權限擋下、建立必定失敗，已修復並實測（`next_doc_no` 改 SECURITY DEFINER；migration 20260720000001 已套用）
- **人員清單空白修復**：9 個頁面（出勤管理全員清單、請假代理人選單、專案成員、薪資對象、HR 設定、文件簽核人、假別餘額等）的人員查詢帶了不存在的 `deleted_at` 欄位，查詢失敗被靜默吞掉、清單一直顯示空白——已全數移除該過濾（活躍判斷本就以 `is_active` 為準）
- **首頁「待確認公告」修復**：首頁公告區塊、公告待確認 API、公告詳情頁對 `document_recipients` 查詢了不存在的 `id` 欄位導致永遠空白，改用 `document_id` 複合鍵並同步修正前端
- **請假頁修復**：使用者未設部門時，代理人查詢會送出空字串條件而失敗，已加防呆（無部門時代理人清單為空）

### Notes
- 這批問題自 4 月起即存在，因查詢錯誤被 `?? []` 靜默吞掉、頁面「看起來只是沒資料」而未被 UAT 發現；已於 7/20 由日誌掃描找出
- 補打卡「不可核准自己的申請」為職責分離的預期行為（核准人＝直屬主管或 admin）；簽核中心對本人申請的按鈕呈現待後續 UX 改善

## v0.8.5 — 待決 low/medium 清理（2026-07-11）

### Fixed
- **功能開關徹底生效**：資產、每日報告、到離職、招募的 API 在模組關閉時也會擋下（先前僅前端頁面擋、直打 API 仍可用）
- **並發保護**：加班核准改為單一原子操作（避免雙重核准）；採購「驗收單→採購請款單」轉單防重（避免重複付款）；分期請款單期數唯一；試劑入庫失敗時回滾，不留孤兒批次
- **計算修正**：跨月無薪假只扣當月天數；投保級距權限頁對齊
- **輸入驗證**：資產、公司行事曆的編輯改白名單驗證，不再外洩資料庫錯誤訊息
- **體驗**：每日報告 KPI 在 600ms 內切換日期不再丟失未存數值；到離職清單反勾已完成項目會同步回退狀態；請假行事曆初次載入與換月顯示範圍一致；補打卡待審可見範圍改用即時主管關係；文件審核的通知/索引只在真正核准/退回時觸發
- **稽核**：資產借用/歸還記錄借用人；審計日誌的動作篩選選項對齊實際紀錄（含 AI 翻譯、OCR、催確認等）

### Notes
- migration 20260711000020（採購請款防重/分期唯一）、20260711000021（資產借用人）、20260711000022（補打卡可見範圍 RLS）已套用
- **擱置**：「一張驗收單對多張入庫單」的防重待確認採購分批入庫規則後再定；相關 app 層防重已撤回

## v0.8.4 — 逐模組深掃修補（2026-07-11）

### Fixed（重大）
- **專案模組全面修復**：專案列表、建立、詳情頁先前因查詢欄位不符而全部無法使用，現已修復
- **請假審核動線修復**：主管與 HR 先前在請假頁看不到「待審核」分頁、只有 Admin 能批假，現已恢復；主管核准時假期餘額先前不檢查也不扣除（可無限請假），已修正為正確檢查與扣除
- **請假天數防竄改**：請假天數改由系統依起訖日與半天重算，不再信任前端傳值
- **薪資修復**：年度報表空白、扣除額/實發顯示「—」已修；手動建立不會再把已進審核/發薪流程的薪資重置回草稿
- **勞健保級距**：改依「涵蓋薪資的投保級距」計算，跨級距薪資不再少扣保費

### Security
- **文件/公告發布**：公告發布端點補上 MFA、限公告類文件、且不可核准自己上傳的文件
- **人員管理**：HR 不得停用或修改 Admin 帳號、不得把他人提升為 Admin
- **績效考核**：進一步堵住可繞過系統直接改考核的縫隙（含身為 HR/Admin 的本人自評自核、跳關、略過 MFA）
- **採購**：手動入庫防止同一驗收單重複入庫（庫存加倍）；送簽者本人不得核准自己送出的採購單

### Fixed（其他）
- 專案加班超時的營運長通知先前對一般員工從不發送，已修正
- 出勤團隊總覽「依部門篩選」先前失效（顯示全部門），已修正
- 營運洞察的請購金額不再把草稿/審批中的單計入
- 加班費率設定的編輯權限判定修正；審計日誌搜尋框恢復作用
- 公告未確認紅點：狀態已非公告的項目不再殘留
- 教育訓練年度時數改以台北時區判定完成年度

### Docs
- 說明文件更正「意見回饋為匿名」的描述（實際為具名、僅 Admin 可見），三語同步

### Notes
- 需套用 migration `20260711000010_perf_guard_hardening.sql`（績效 guard 強化）
- 少數低風險項目列為後續處理（見 ROADMAP）

## v0.8.3 — 第三輪完整審查修補（2026-07-11）

### Security
- **績效考核越權修補**：修正員工可繞過系統介面、直接透過 API 竄改自己考核的主管評分／自我打滿分／自我完成的漏洞（補上資料庫層的欄位守衛，與請假／加班／報帳同級防護）
- **開放重導向修補**：`/api/locale` 語言切換只接受站內路徑，杜絕被構造成導向外部釣魚網站

### Fixed
- **每日摘要待簽數**：一般主管的 Teams 每日摘要先前會顯示「全公司待簽假總數」，現在只算自己直屬部屬的
- **薪資列表不刷新**：HR／財務／營運長在薪資審核流程按核准後，列表狀態現在會即時更新（先前需手動重整；也修正因此誘發的重複送出）
- **簽核中心防重複點擊**：核准／駁回／撥付按鈕在處理中會禁用，避免重複送出
- **薪資／加班輸入驗證**：手動建立薪資的金額、加班的日期時間格式與時數上限，後端一律驗證，防止異常值進入計算
- **AI 問答效能**：全文模式限縮查詢筆數，文件庫變大時不再每次下載整庫

### Docs
- 說明文件（/help）修正多處與實際權限/狀態不符的描述（三語）：薪資狀態五階段流程、薪資查看範圍、文件與合約核准權限（需 approve_contract + MFA）、公告分類（緊急／行政／法規／HR）、報帳匯出權限

### Notes
- 需套用資料庫 migration `20260711000001_perf_update_guard.sql`（績效欄位守衛）

## v0.8.2 — 系統效能升級：伺服器遷至東京（2026-07-09）

### Changed
- **伺服器搬到與資料庫同一區域（東京）**：原本應用程式跑在美東、資料庫在東京，每個操作都要跨太平洋來回；現在同區運行，頁面載入與各項操作（打卡、送單、查詢）的反應速度預期顯著提升（參考另一專案同樣調整後，資料庫往返延遲從約 500ms 降到約 100ms）

## v0.8.1 — 安全與正確性掃修（2026-07-09）

### Security
- **向量檢索限定政策類文件**：向量索引與檢索僅涵蓋規章／公告／內部文件，合約、NDA、MOU 等機密文件不入索引（索引端與資料庫端雙重過濾），杜絕機密內容經 AI 問答外洩的可能
- **API 金鑰不再送達瀏覽器**：系統設定頁的 AI／Teams 金鑰改為只顯示「已設定／未設定」，真實金鑰不進入前端網頁內容；要更換直接輸入新值即可（留空不會清除）

### Fixed
- **跨午夜且跨日別的加班計薪**：如週五 22:00–02:00 的加班，跨入週六的時段現在會依「休息日」費率分段計價（先前整段都按起始日日別計）
- **文件索引改為背景執行**：文件核准、OCR、翻譯的回應不再等待向量索引完成
- **OCR 錯誤更明確**：無文字的掃描檔回「內容為空」而非伺服器錯誤；OpenAI 相容端點遇 PDF 在上傳前即提示不支援
- **說明文件更新**：加班條目補上日別與勞基法分段說明；OCR 條目移除過時的端點設定說法

### Notes
- 月薪制國定假日加班「外加 2 倍」是否調整為「外加 1 倍」（避免與月薪內含的當日工資重複），待 HR 確認口徑——如需調整，直接把費率設定中的「國定假日」倍率改為 1.00 即可

## v0.8.0 — AI 政策問答向量檢索（2026-07-08）

### Added
- **向量檢索（RAG）**：AI 政策問答改用 pgvector 語意檢索——文件切段建立向量索引，提問時取最相關段落作答，文件量大時仍精準快速；未設定 embedding 時自動退回原本的全文模式
- **Embedding 設定**：「AI 連線」卡片新增 Embedding 小節（模型必填才啟用；端點與 Key 留空自動沿用 AI 連線）；測試連線按鈕會一併實測 embedding 並顯示結果與耗時
- **重建文件索引按鈕**：初次設定 embedding 後一鍵回填既有文件的索引；之後文件核准、OCR、AI 翻譯完成時會自動增量更新索引

### Notes
- 向量索引僅含已核准未刪除文件；段落表僅服務端可存取，讀取授權一律走 API 層

## v0.7.8 — AI 設定整併：一條 AI 連線（2026-07-08）

### Changed
- **AI 設定大幅簡化**：原本 7 個欄位（兩把 Key、獨立 OCR 端點）整併成一張「AI 連線」卡片——供應商下拉（OpenAI / Anthropic / Google Gemini / 自訂 OpenAI 相容）、API Key、模型（留空用預設）；端點欄位只在選「自訂」時出現
- **測試連線按鈕**：一鍵實測，按鈕旁顯示成功（所用模型＋耗時）或失敗原因，附測試時間；結果會保存，重新整理後仍可見上次測試紀錄
- **AI 功能狀態一覽**：設定卡下方直接顯示 AI 翻譯／AI 政策問答／文件 OCR 目前是「可用」還是缺什麼（未設 Key、flag 未開）
- **文件 OCR 改用 AI 連線的視覺模型**：不再需要獨立 OCR 服務端點；Gemini／Anthropic 支援圖片與 PDF，OpenAI 相容端點支援圖片
- 舊的 Gemini API Key 設定自動搬移到新的 AI 連線後移除

## v0.7.7 — AI 供應商通用設定（2026-07-08）

### Changed
- **AI 設定不再綁定 Gemini**：系統設定新增「AI 供應商」（openai / anthropic / gemini）、「AI API Key」、「AI API 端點」（選填，支援 OpenAI 相容端點如 Groq / Ollama / LiteLLM / 自架 proxy）、「AI 模型」（選填，未設用供應商預設）。AI 翻譯與 AI 政策問答皆改走此設定
- 舊的 Gemini API Key 設定保留、向下相容（新 Key 未設時自動沿用）

### Notes
- AI 功能（翻譯／政策問答／OCR）程式碼皆已完成，啟用只需在系統設定填入對應的 Key／端點

## v0.7.6 — 加班費依勞基法分段計算（2026-07-08）

### Added
- **加班日別**：加班申請新增「加班日別」（工作日／休息日／國定假日），選日期時自動判斷（週六日＝休息日），國定假日手動選擇；申請列表顯示日別
- **勞基法分段計薪**：薪資計算的加班費改依勞基法 §24/§39 分段——工作日前 2 小時 ×1.34、後 2 小時 ×1.67；休息日前 2 小時 ×1.34、第 3–8 小時 ×1.67、第 9 小時起 ×2.67；國定假日 ×2。倍率仍可由管理員在加班費率設定中調整

### Fixed
- **加班費先前一律以 1.34 倍計算**：因加班單未記錄費率，所有加班（含休息日後段、國定假日）都被以最低倍率計薪，現已修正；歷史加班單已回填日別（週六日＝休息日），已發薪月份不重算
- **HR 設定頁**：假別清單、假期餘額的假別下拉、加班費率清單先前因欄位問題永遠空白，已修復

### Changed
- **加班費率設定頁**：移除無作用的「啟用」開關（費率為法定分段，只可調倍率不可停用）；費率名稱依介面語言顯示

### Notes
- 例假日出勤屬天災事變特殊情況（勞基法 §40，加倍發給＋補假），不納入系統自動計算，如遇到請人工處理

## v0.7.5 — 全面品質掃修 / 選單排序 / 說明文件改版（2026-07-08）

### Fixed（全站 13 個面向的地毯式審查，43 項確認問題全數修復或列管）
- **意見回饋**：送出必定失敗的問題已修復（欄位與類別值對齊資料庫），管理端狀態流也一併修正
- **請假**：假別清單載入失敗、假期餘額顯示錯誤已修復；主管／HR 現在能正確看到待審假單；特休等年資制假別的餘額上限檢查補齊（申請與核准兩端）
- **加班**：跨午夜加班不再算成 0 小時（畫面預覽同步修正）；加班單現在會正確出現在簽核中心與營運儀表板
- **出勤**：管理端出勤總覽不再一片空白；補打卡的時間不再差 8 小時；HR 主管可正常進入出勤管理與班別管理頁
- **薪資**：管理員手動建立薪資記錄失敗的問題已修復
- **簽核防護**：報帳、文件／合約、績效考核全面加上「不可核准自己的申請」防護（與請假／加班／出差一致）
- **公告／文件**：hr_manager 發布公告被系統擋下的問題已修復；催讀 4 小時冷卻可被繞過的漏洞已補；所有稽核記錄（上傳／核准／翻譯／確認閱讀／催讀／採購簽核）先前寫入靜默失敗，現在會確實留痕
- **Outlook 同步**：請假／出差核准後的行事曆事件先前一律建立失敗（全天事件格式錯誤），已修復
- **假別／加班費率管理**：新增、編輯、清單顯示全面修復（先前儲存必失敗、清單永遠空白）
- **採購**：多位審核者同時核准可能重複執行後續動作的併發問題已修復；驗收單重複轉入庫、轉單帶入超量的問題已修復；請購單的進貨進度現在會隨入庫過帳正確更新
- **資產**：同一張驗收單可被重複轉成資產的問題已修復
- **每日報告**：切換日期偶發顯示錯誤日期資料、KPI 輸入競態、已刪除群組成員仍看到建立任務按鈕等問題修復
- **儀表板**：待審請假／加班計數永遠為 0 的問題已修復；加班卡片標籤誤植「待審合約」已更正
- **介面文字**：補齊缺漏的翻譯（績效週期刪除提示、使用者權限開關標籤），三語同步

### Changed
- **左側選單重新排序**：人事管理（每天用的打卡／請假）移到文件管理之前，區內依使用頻率排序；管理後台選單依領域分組（組織 → 出勤 → 財務／營運 → 系統）；手機版「更多」選單同步調整
- **說明文件（/help）全面改版**：涵蓋全部 24 個功能模組（新增簽核中心、行事曆、每日報告、出差、報帳、教育訓練、績效、採購、資產、試劑耗材、營運儀表板等條目），權限矩陣擴充至 13 區 46 列，三語內容同步更新

### Notes
- 一支資料庫補強 migration（資產轉入防護）已備妥待授權後套用；「加班倍率自動計算」需資料表調整，已列入 roadmap 待決事項

### Added
- **文件 OCR 全文搜尋**：純檔案（掃描 PDF / 圖片）文件可由文件管理者一鍵 OCR 抽取文字，之後可在文件搜尋中被找到，AI 政策問答也能引用；OCR 服務端點由管理員在系統設定中配置
- **彈性工時班別**：管理員可定義多種班別（上下班時間、工作日、彈性寬限）並指派給員工；打卡遲到改以員工當日班別的上班時間判定（未指派者沿用預設上班時間）
- **打卡地理圍欄**：管理員可設定允許打卡的辦公室範圍（可多個地點），開啟「強制範圍檢查」後，超出所有範圍將無法打卡；預設關閉（僅記錄座標不阻擋）

### Notes
- OCR 需管理員先在系統設定填入 OCR 服務端點；地理圍欄強制預設為關閉，需管理員手動開啟

## v0.6.9 — Outlook 行事曆同步（2026-07-07）

### Added
- **請假 / 出差自動同步至 Outlook**：請假或出差經核准後，系統會自動在當事人的 Outlook 行事曆建立整天「不在辦公室」事件；退回則自動移除。單向同步（Outlook 端的變更不會回寫 myOPS），使用各自的 Microsoft 帳號授權。

### Notes
- 首次啟用需**重新登入一次** myOPS（以授權行事曆存取）；未連結者核准流程不受影響，只是不會建立 Outlook 事件

## v0.6.7 — 合約/公告流程強化與資產轉入（2026-07-07）

### Added
- **合約到期自動提醒**：合約（含 NDA/MOU/增補）到期前 90 天與 30 天，系統自動透過 Teams 提醒具合約審核權限的同仁
- **合約審核通知**：合約核准或退回後，自動通知申請人結果；合約類文件另會知會營運長
- **上傳合約時關聯提示**：選定公司後若該公司已有文件，可一鍵關聯既有文件
- **公告一鍵催人**：發布者可在報表對尚未確認的同仁一鍵發送 Teams 提醒（附冷卻避免重複打擾）
- **公告簽署清單匯出**：發布者可將某公告的閱讀確認清單匯出成 Excel
- **公告提醒頻率**：每日提醒改依各公告設定的提醒間隔（reminder_days），不再每天重複提醒
- **專案加班超額通知**：專案加班時數超過設定門檻時，自動通知營運長
- **採購驗收單一鍵轉資產**：已核准的進貨驗收單可直接轉為資產，自動帶入廠商 / 金額 / 日期

### Fixed
- **公告閱讀確認**：修正「發布公告並指定需確認收件人 / 確認閱讀 / 每日提醒」因資料表欄位缺漏而無法運作的問題

## v0.6.4 — 稽核修復與行事曆隱私（2026-07-06）

### Fixed
- **請假 / 加班送出**：修正送出申請因欄位名稱與資料庫不符而一直失敗的問題（此前無法成功建立任何請假或加班申請）
- **加班列表 / 待審清單**：修正因查詢不存在欄位而載入失敗

### Security
- **行事曆隱私**：修正任何員工可讀取他人請假事由 / 附件 / 出差行程的問題；行事曆改為只顯示必要資訊（姓名、日期、假別），請假行事曆一般員工僅見同部門
- **採購作廢**：作廢已核准單據改為限採購管理者 / 管理員，並需 MFA（原本唯讀權限即可作廢並回沖庫存）
- **驗收單編輯**：補上建檔人 / 管理者所有權檢查（原本任何採購人員可改他人草稿並自任簽核人）
- **薪資簽核**：核准動作驗證目前階段，不可跳關或倒退
- **補打卡**：核准人改以主管關係認定、不可核准自己的申請、申請人無法自行指定核准人

### Fixed（時區）
- 打卡日期、打卡提醒、資產 / 證照到期提醒改以台北時區計算，凌晨時段不再差一天

## v0.6.2 — 全站安全稽核修復（2026-07-05）

### Security
- **權限提升漏洞（重大）**：修正任何登入者可透過 API 直接把自己的角色改為管理員 / 加開權限的問題（users 資料表 self-update 未限制敏感欄位）
- **自我核准漏洞**：修正申請人可繞過主管、自行核准自己的請假 / 加班單；文件（合約）核准補上 MFA 驗證，並限定僅具審核權限者可變更文件狀態
- **報帳 / 出差取消失效**：修正一般員工無法取消自己待審申請（RLS 缺 WITH CHECK 導致必定失敗）
- **履歷外洩**：招募履歷檔案改為僅 HR / 管理員可存取（原本任何登入者皆可下載）

### Fixed
- **請假相關查詢**：修正請假列表 / 簽核中心 / 行事曆因查詢不存在的欄位而失敗的問題
- **試劑耗材庫存**：批次異動改為單一交易的原子操作，避免併發時數量錯亂、超領、或紀錄與庫存不一致
- **營運儀表板**：近 6 個月統計改以台北時區計算，月初不再整組偏移
- **報帳差旅串接**：送出報帳時驗證所選出差單屬本人且已核准
- 訓練時數、證照、資產軟刪除等權限收緊

## v0.6.1 — 績效考核（2026-07-05）

### Added
- **績效考核**：新頁面 `/performance` — HR 建立考核週期；員工設定目標（權重合計 100%）→ 主管核定或退回 → 員工逐目標自評（1–5 分）→ 主管逐目標評分與總評（需 MFA）→ 完成鎖定結果；完成時自動存入該期間的每日報告 KPI 快照（目標 vs 實績）供對照；HR 可檢視全公司進度並重新開啟已完成考核
- UAT 清單 v1.3：新增第 28 章績效考核測試（T-105 ~ T-112，共 112 項）

### Notes
- feature flag `performance` 預設關閉；DB migration（`20260704000004`）待另行執行後才可開啟

## v0.6.0 — 招募管理 + 試劑/耗材管理（2026-07-04）

### Added
- **招募管理**（HR/管理員限定）：新頁面 `/admin/recruiting` — 職缺管理、應徵者追蹤（投遞→書審→面試→Offer→錄取/未錄取）、履歷上傳、面試評分與回饋記錄
- **試劑/耗材管理**：新頁面 `/lab` — 試劑與耗材品項、批次管理（批號＋效期）、使用/開封/報廢異動記錄、低庫存與效期到期提醒；全員可查閱庫存，具「試劑耗材管理」權限者可異動

## v0.5.9 — 入職/離職流程（2026-07-04）

### Added
- **入職/離職流程**（HR/管理員限定）：新頁面 `/admin/lifecycle` — 為新進或離職員工建立流程清單（入職 10 項/離職 8 項預設範本：帳號、設備、權限、交接、人資），逐項勾選追蹤（記錄完成人與時間）、可加自訂項目與備註、完成後結案

## v0.5.8 — AI 政策問答（2026-07-03）

### Added
- **AI 政策問答**：說明頁（/help）新增問答框 — 用自然語言詢問公司規定（如「特休怎麼計算」），AI 根據已發布的公司文件回答並附出處；文件中沒有的內容會明說並建議洽詢 HR
- **Teams bot 升級**：對 bot 提問不在既有指令範圍的問題時，自動以文件庫為根據回答

## v0.5.7 — 營運儀表板（2026-07-03）

### Added
- **營運儀表板**（管理員限定）：新頁面 `/insights` — 本月出勤與加班摘要、年度請假與報帳合計，以及近 6 個月加班時數、出勤人日、請購金額趨勢與請假假別/報帳類別/專案加班分布圖

## v0.5.6 — 公司行事曆（2026-07-03）

### Added
- **公司行事曆**：新頁面 `/calendar` — 一頁月曆彙總公司活動（綠）、已核准請假（藍）、已核准出差（紫）；點日期看當日完整清單；HR/管理員可建立與管理公司活動；手機版最佳化

## v0.5.5 — 出差管理（2026-07-03）

### Added
- **出差管理**：新頁面 `/business-trips` — 出差申請（目的地、事由、行程）→ 主管審批；申請人可取消待審核申請
- **簽核中心整合**：出差申請納入 `/approvals` 一鍵核准/退回
- **差旅報帳串接**：已核准出差一鍵帶入報帳表單（類別與事由自動預填），報帳記錄顯示關聯出差

## v0.5.4 — 教育訓練與證照管理（2026-07-03）

### Added
- **教育訓練**：新頁面 `/training` — 訓練課程建立與指派（含教材連結、必修標記）、員工標記完成並上傳結業證明、年度累計時數統計、課程完成進度總覽
- **證照管理**：員工證照檔案（GCP、生安、輻安等）登錄、到期日追蹤；30 天內到期顯示「即將到期」標示；管理者「到期提醒」分頁列出 60 天內到期證照
- **儀表板**：新增「證照將到期」卡片（訓練管理者限定）
- 新增可指派權限「訓練與證照管理」（training_manage）

## v0.5.3 — 資產與儀器管理（2026-07-03）

### Added
- **資產與儀器管理**：新頁面 `/assets` — 資產台帳（IT 設備/實驗儀器/傢俱）、保養/校驗/維修/領用/歸還記錄（含附件）、校驗與保養到期追蹤；保養或校驗記錄完成後自動排下次到期日；領用/歸還自動更新保管人
- **到期提醒**：資產頁「到期提醒」分頁列出 60 天內到期項目；儀表板新增「校驗/保養將到期」卡片（30 天內）
- 新增可指派權限「資產管理與記錄」（asset_manage）；一般員工可檢視資產清單（唯讀）

### Fixed
- **權限指派清單對齊**：修正多個在管理介面可指派、但系統從未檢查的無效權限（如舊的薪資檢視 key）— 現在清單上的每個權限都真實生效；薪資的檢視/財務確認/最終核准三種權限現在可正確指派

## v0.5.2 — 統一簽核中心（2026-07-03）

### Added
- **簽核中心**：新頁面 `/approvals` 彙總所有待你審核的申請 — 請假、加班、補打卡、報帳、文件/合約、薪資、採購 — 不用再逐頁巡，一鍵核准或退回
- **補打卡審批**：主管現在可以核准/退回補打卡申請，核准後自動寫入當日出勤記錄（此前補打卡送出後無人能處理）
- **儀表板**：具報帳審批權限者的「今日待辦」新增「待審報帳」卡片

### Security
- 修復文件更新 API 缺少授權檢查的問題（原本任何登入者皆可變更文件狀態），現限管理員與具合約審核權限者

## v0.5.1 — 員工報帳模組（2026-07-03）

### Added
- **員工報帳**：代墊費用線上申請 — 選類別（交通/差旅/誤餐/用品/其他）、上傳發票照片或 PDF、送出後由具審批權限的同仁核准與撥付；申請人可隨時查看進度、取消待審核的申請；財務可一鍵匯出 Excel 明細
- 新增可指派權限「報帳審批與撥付」（expense_approve），審批動作需完成 MFA 驗證

## v0.5.0 — 品質修正與細節優化（2026-07-03）

### Fixed
- **無權限導向一致化**：專案詳情頁與每日報告團隊頁在沒有權限時，統一顯示水墨山水無權限頁面（原本靜默跳回列表頁）
- **每日報告 — KPI 輸入**：清空欄位不再誤存為 0；輸入完成才儲存，儲存失敗會顯示錯誤
- **每日報告 — 日期時區**：「今天」改以台北時間計算，海外或跨時區使用不會差一天

### Changed
- 內部程式碼品質整理（lint 歸零、移除殘留檔案）

## v0.4.9 — 權限系統重大修復（2026-07-02）

### Fixed
- **一般員工看不到任何功能**：修正 feature flags 讀取權限問題 — 非管理員登入後所有模組（打卡、請假、文件、每日報告等）都無法使用、被導向無權限頁面。現在所有員工都能正常看到已開啟的功能模組
- **每日報告 — 子任務勾選無法儲存**：勾選後重新整理會恢復原狀，已修正儲存路徑
- **每日報告 — 成員任務列表載入失敗**：修正查詢語法，成員現在能正常看到被指派的任務
- **每日報告 — 操作失敗時仍顯示成功**：任務完成、確認、刪除等操作現在會正確檢查結果，失敗時顯示錯誤提示

### Security
- **每日報告權限收緊**：viewer 僅能管理自己群組的任務（原本可跨群組）；成員僅能更新任務完成狀態（原本可改任何欄位）；已刪除群組的 viewer 不再能存取前成員資料

## v0.4.7 — 無權限頁面優化（2026-06-22）

### Changed
- **無權限頁面**：說明文字與返回按鈕移至詩詞上方，操作更直覺；深色模式改為水墨深夜色調；手機版觸控按鈕加大至 44px，竹枝裝飾適配小螢幕

## v0.4.6 — 無權限頁面上線（2026-06-22）

### Added
- **無權限頁面**：沒有存取權限時不再靜默跳回首頁，改顯示水墨山水畫風格頁面，引用葉紹翁《遊園不值》「小扣柴扉久不開」，說明無法進入的原因，並提供返回按鈕

## v0.4.3 — 頁面測試中標示（2026-06-18）

### Changed
- **測試中標示**：各功能頁面標題旁統一顯示黃色「測試中」提示框，提醒現階段為員工測試期

## v0.4.2 — 手機導覽優化（2026-06-18）

### Changed
- **手機版底部導覽**：「每日報告」移至「更多」面板首位，方便快速存取

## v0.4.1 — 側欄導覽優化（2026-06-18）

### Changed
- **側欄**：「每日報告」區段移至文件管理之上，符合日常使用優先順序

## v0.4.0 — 每日報告模組上線（2026-06-18）

### Added
- **每日報告模組**：業務同仁可在 myOPS 直接填報每日行程、完成回報、KPI 數值，取代原有獨立網站
- **每日填報**：行程 / 完成回報 / KPI 三個分頁，支援一鍵套用常用樣板
- **我的任務**：查看主管指派的任務與子任務，完成後可直接標記回報
- **團隊總覽**：主管（Viewer）可查看所屬群組所有成員的當日報告
- **群組管理**（管理員）：可建立群組、指派成員與主管，彈性對應各部門結構

### 操作說明
管理員請先至**設定 → 功能開關**啟用「每日報告」，再至**每日報告群組**建立群組並指派成員。

## [0.3.8] - 2026-06-15

### Changed
- **Teams 通知改走 Dr.Ave Gateway**：myOPS 不再各自直連 Bot Framework，所有通知（請假結果、採購簽核、打卡提醒、每日摘要、公告、薪資單）改呼叫共用閘道 `drava.cancerfree.io/api/notify`；收件人以 email 識別、查無 conversation reference 時優雅略過

### Added
- **Teams 可操作簽核卡片**：簽核請求以帶按鈕的卡片送出，主管可直接在 Teams 核准/退回
- **一鍵簽核政策後台**（`/admin/bot-policy`）：管理員逐功能開關「Teams 一鍵直簽」、金額型單據設門檻；預設全關（走深連結 + MFA，最安全）
- **bot-facing API**：`/api/bot/approve`、`/api/bot/query`（Dr.Ave 以 `BOT_GATEWAY_TOKEN` 回呼）；一鍵簽核執行前再驗政策門檻，audit 標記 `via=teams_one_tap`

### Removed
- myOPS 自有 Teams webhook（`/api/teams/bot`）— 已由 Dr.Ave 集中承載

## [0.3.7] - 2026-06-15

### Changed
- **採購列表全面強化**：9 個列表（詢價/請採購/進貨驗收/出入庫×3/請款×3/廠商/商品/評估×2/待簽收件匣）統一加上
  - 欄位排序（點標題切換，金額/數量/日期依型別正確排序、空值排最後）
  - 即時搜尋（涵蓋表內全部可見欄位；詢價/請採購另保留伺服器端單號/廠商搜尋）
  - 每頁 20 筆分頁（搜尋/排序自動回第一頁，單頁時自動隱藏）

## [0.3.6] - 2026-06-12

### Changed
- **採購總覽改版為 Dashboard**：模組卡片附即時筆數（廠商/商品/各單據），近效期批號（60 天內）黃色警示卡直達庫存頁；「我的待簽」保留置頂
- **詳情頁返回**：所有採購詳情頁統一加「返回」（記住上一頁，無歷史則回列表）
- **移除頁尾**：版本與部署時間 Sidebar 已有，頁尾資訊列移除

## [0.3.5] - 2026-06-12

### Added
- **採購模組 Phase C：Ragic 歷史資料匯入完成（19 張表 4,768 筆，筆數 100% 吻合）**
  - 主檔：倉庫 6、廠商 50、商品 238、廠商報價 367、批號庫存 240
  - 單據：詢價 231、請採購 253（明細 425）、進貨驗收 266、入庫 240（明細 294）、出庫 534（明細 633）、請款 16+161+13、評估 49+243
  - 庫存分類帳回填 927 筆異動；單號沿用 Ragic 原編號；舊單號對照表（ragic_id_map）2,952 筆
  - FK 完整性全數通過；簽核時間軸對歷史單顯示「Ragic 歷史單據」標記
  - 匯入工具入庫（`scripts/import-ragic/`，支援 dry-run）

### Chore
- 已知待處理（不影響上線）：6 筆簽核中歷史單需人工重送簽；20 個批號存在 Ragic 時期盤差（在庫量以 Ragic 主檔為準已正確）；11 位 Ragic 經手人在 myOPS 無帳號，歷史單據暫掛管理員名下（原名保留於備註）

## [0.3.4] - 2026-06-12

### Added
- **採購模組 Phase B（完整單據鏈，feature flag 仍關閉）**
  - 八種單據完整 CRUD + 表單：詢價單（簽核中欄位鎖定）、請採購單（明細編輯器、金額自動計算）、進貨驗收單（訂金區塊）、入庫/出庫單、訂金/採購/分期請款單
  - **轉單**：六條轉換（詢價→採購→進貨→入庫；採購→訂金請款；進貨→請款→分期），PR→GR 自動帶入已過簽訂金資訊
  - **庫存過帳**：原子交易 Postgres 函式（批號自動判斷加量/新建、寫入分類帳、冪等保護、超扣攔截、可沖銷）
  - **條碼掃描（mobile-first）**：掃描槍 + 手機相機（BarcodeDetector / zxing fallback），入庫出庫掃碼自動加減量、震動回饋
  - **作廢並複製**：過簽單據可作廢重開（GR 有下游請款/入庫時清楚攔截提示）——解決舊系統簽核後無法修正的痛點
  - **商品出入庫分類帳頁**：年度流水 + 結餘、批號在庫與效期警示、總量以庫存單位一致顯示——解決舊系統紀錄要手動整理的痛點
  - prod 完整鏈 SQL e2e 驗證 25/25 通過

## [0.3.3] - 2026-06-12

### Added
- **採購模組 Phase A（地基，feature flag 預設關閉）**：自 Ragic 採購系統 migrate 的第一階段
  - 資料庫：21 張新表（廠商/商品/廠商報價主檔、倉庫/批號庫存、庫存分類帳 `stock_movements`、10 種單據骨架、簽核步驟表），已套用至 prod，RLS 全啟用
  - 單據自動編號：`next_doc_no()`（前綴-年月-流水，並發安全），BEFORE INSERT trigger 自動產號
  - **多關卡簽核引擎**：10 條簽核鏈（角色制：部門主管/COO/CEO/會計），簽核人支援角色/直屬主管/單據欄位動態/任意確認四型；核准拒絕沿用 MFA（AAL2）閘 + 審計日誌 + Teams 通知；過簽自動執行登錄（廠商評估→廠商清冊、商品評估→廠商商品價格）
  - 商品主檔導入**雙單位制**（採購單位/庫存單位/換算率），解決報價以箱、出庫以瓶的單位不一致問題
  - 頁面：採購總覽（我的待簽收件匣）、廠商主檔、商品主檔、廠商/商品審核評估（含簽核時間軸）
  - 權限：新增 `ceo` 職務角色與 `procurement_unit` / `procurement_manage` / `procurement_payment_approve` 三個特殊權限
  - i18n：`procurement` 命名空間 256 個 key 三語同步
- 後續：Phase B（詢價→採購→進貨→出入庫單據鏈、條碼掃描、作廢重開、出入庫分類帳頁）、Phase C（Ragic 歷史資料匯入）完成後開啟功能

## [0.3.2] - 2026-06-11

### Docs
- **使用說明書上線**（`docs/generated/`）：user / admin 兩種角色 × 中／英／日三語共 6 份，含工作流程圖（mermaid）與 FAQ
- **PRD 與現況同步**：PRD.md 任務清單 57 項逐一對照程式碼勾選（3 項標註未實作）、新增現況說明章節；Teams Bot PRD（myOPS-PRD-v0_3.md）標記結案（T64–T73 完成，T63 待 Azure 手動設定）

## [0.3.1] - 2026-06-11

### Added
- **Teams Bot 整合（PRD T63–T72）**：提醒與通知正式接上 Microsoft Teams
  - `src/lib/teams-bot.ts`：Bot Framework token 快取與 proactive messaging（無 conversation reference 安靜跳過，發送失敗不影響主流程）
  - `/api/teams/bot` webhook：驗證 Bot Framework JWT（jose），`conversationUpdate` 時以成員 email 對應使用者並儲存 conversation reference
  - 接線完成：每日待辦摘要（T67）、上下班打卡提醒（T68）、即時通知（T69）、請假審核結果（T70）、薪資單發出（T71）、公告發布（T72）
  - 訊息依**收件人**的 `users.language` 三語發送（新增 `teamsMessages` 命名空間）
  - `teams_conversation_references` migration 與 `docs/teams-bot-setup.md`（Azure Bot 設定手冊）

## [0.3.0] - 2026-06-11

### Fixed
- **Cron 端點被登入攔截**：`proxy.ts` 把 Vercel Cron 的請求（無 session cookie）一律 307 轉向 /login，排程永遠打不到 route——三個 cron 端點（clock-reminder、daily-digest、notify）加入 middleware 豁免清單，依賴各自的 fail-closed CRON_SECRET 驗證（payroll 端點不豁免，維持 MFA 強制檢查）

### Chore
- 版本進位：依新規則（patch 最大 9）自 v0.2.50 進位至 v0.3.0

## [0.2.50] - 2026-06-11

### Added
- **平板 Drawer 側欄**：md–lg 區間新增頂部列 + 漢堡選單，點擊滑入完整側欄（遮罩關閉、ESC、路由切換自動收合），桌面與手機行為不變
- **Vercel Cron 排程**（`vercel.json`）：每日待辦摘要平日 08:30、打卡提醒平日 07:00（上班）/ 17:30（下班）（台北時間）；cron route 補上 GET handler 並依台北時間自動判斷提醒類型

### Changed
- **程式碼品質**：清除全部 248 個既有 ESLint 問題（`any` 改為實際型別、移除未用變數、effect 內 setState 重構、hook 依賴修正；僅保留 1 處經評估不可安全改動者），並修正公告列表搜尋競態問題
- **CI**：GitHub Actions 升級 checkout/setup-node v5、Node 24；新增 `.gitattributes` 強制 LF 換行

### Security
- **Cron 端點改為 fail-closed**：`notify`、`clock-reminder`、`daily-digest` 在 `CRON_SECRET` 未設定時一律拒絕（原為 fail-open）——部署需設定 `CRON_SECRET` 環境變數

## [0.2.49] - 2026-06-10

### Fixed
- **API 錯誤訊息多語化**：21 個 API route 的錯誤回應改為依使用者語言回傳（next-intl 伺服器端翻譯，新增 `apiErrors` 命名空間；Teams 機器人訊息與匯出報表內容維持原樣）
- **觸控目標**：按鈕、側欄、底部導覽在觸控裝置上保證 ≥44px（Tailwind `pointer-coarse:` variant，桌面密度不變）
- **表格手機橫向捲動**：出勤紀錄與使用者列表表格補上 `overflow-x-auto`
- **Dark mode 對比**：移除 16 處 `text-white` 直書（改 `text-gray-50` 或補 `dark:` variant），修正勞健保上傳區與出勤管理表頭的深色模式對比

### Security
- `/api/teams/notify` 加上 CRON_SECRET Bearer 驗證（比照其他 cron 端點）

## [0.2.48] - 2026-06-10

### Fixed
- **i18n 全面補洞**：抽出 22 個元件中約 218 條 hardcode 中文字串，改為 next-intl `t()` 翻譯鍵，zh-TW / en / ja 三語檔同步新增（涵蓋 admin 管理頁表頭、按鈕、placeholder、aria-label、toast 訊息等）
- **回饋管理截圖**（`/admin/feedback`）：原生 `<img>` 改為 `next/image`（fill + aspect-video 容器，避免 layout shift）

### Added
- **Footer**：掛載至 dashboard layout，依 PRD 固定格式顯示 `坂本 | v{version} | Deployed: {time}`（讀取 `NEXT_PUBLIC_AUTHOR_NAME` / `NEXT_PUBLIC_APP_VERSION` / `NEXT_PUBLIC_DEPLOY_TIME`）

### Chore
- 三語訊息檔結構驗證一致（各 995 個 leaf key）；`npm run build` 通過

## [0.2.47] - 2026-04-10

### Changed
- **HR 管理頁面**（`/admin/hr-settings`）：直接嵌入假別管理、假別額度、加班費率、出勤異常、獎金管理功能，移除外部連結
- **財務管理頁面**（`/admin/finance-settings`）：直接嵌入勞健保級距表、薪資異常檢查功能，移除外部連結
- **Sidebar**：移除 7 個已嵌入設定頁的獨立管理項目（leave-types, leave-balances, overtime-rates, attendance-anomalies, bonuses, insurance-brackets, payroll/anomalies）
- **管理組件 readOnly 支援**：LeaveTypesManager、LeaveBalancesManager、OvertimeRatesManager、BonusClient、InsuranceBracketsClient 加上 `readOnly` prop，COO 角色唯讀檢視

### Removed
- `HRManagementLinks` 組件（功能已嵌入 HR 管理頁）
- `FinanceManagementLinks` 組件（功能已嵌入財務管理頁）

## [0.2.44] - 2026-04-08

### Added
- **職能角色系統（job_role）**：新增 `job_role` 欄位（member / hr_manager / finance / coo）
  - DB migration：`users.job_role` 欄位，並將舊 `granted_features` 中的角色標記自動遷移
  - HR 角色：可修改部門/職位/停用帳號，不可更改系統角色
  - Finance 角色：可存取加班費率管理、勞健保級距管理（原本僅 Admin）
  - COO 角色：可管理 COO 設定、唯讀 HR/Finance 設定
- **職能角色預設功能矩陣**（`src/lib/job-role-features.ts`）
- **財務管理頁**新增費率管理連結（加班費率、勞健保級距）
- **使用者管理頁**：HR 角色可存取，但僅能修改允許欄位

### Changed
- `src/lib/features.ts`：移除角色標記（hr_manager / finance_payroll / coo_notify），新增 12 個個別指派功能
- `src/lib/role-settings.ts`：`KEY_OWNER` 對應值改為 job_role 識別碼
- 設定頁存取改為檢查 `job_role` 而非 `granted_features`
- `UserEditForm`：新增職能角色欄位（Admin 才能修改），角色/個別授權僅 Admin 可見
- Sidebar：非 Admin 的 HR/Finance/COO 顯示對應管理頁連結

## [0.2.33] - 2026-04-07

### Fixed
- CI build 修復：將 `feature-flag-keys.ts` 拆出為 client-safe 常數檔
  - `SettingsClient.tsx`（Client Component）原本 import `feature-flags.ts` 導致 `next/headers` 被打包進 client bundle，Turbopack 報錯
  - 現在 client 元件只 import `feature-flag-keys.ts`（無 server 依賴）
  - `Sidebar.tsx` / `BottomNav.tsx` 的 `import type { FeatureFlags }` 同步改為 `feature-flag-keys.ts`

## [0.2.32] - 2026-04-07

### Fixed
- 手機版 BottomNav「更多」面板補上「專案」與「個人設定」連結，與桌面版 Sidebar 功能對齊

## [0.2.31] - 2026-04-06

### Fixed
- Sidebar / BottomNav 語言切換：`supabase.update().catch()` 改用 `Promise.resolve(...).catch()`，修正 PostgREST builder 不實作 `.catch()` 導致的 runtime 錯誤

## [0.2.30] - 2026-04-06

### Added
- 功能開關系統（Feature Flags）
  - Admin 系統設定頁新增「功能開關」區塊，支援 toggle 開啟/關閉各功能模組
  - 9 個可控功能：出勤、請假、加班、薪資、文件、公告、合約、專案、意見回饋
  - 預設：意見回饋開放，其餘關閉（待測試後由管理員手動開啟）
  - Admin 不受開關限制，永遠可存取所有功能
- Sidebar / BottomNav 自動隱藏已關閉的功能連結
- 各功能頁 route 層保護：非 admin 直接打 URL 也會 redirect 回首頁

## [0.2.29] - 2026-04-06

### Added
- 說明文件頁面新增「使用者功能矩陣」表格
  - 6 個功能分區：出勤、請假/加班、薪資、文件管理、公告/合約、專案/其他
  - 4 種角色權限：一般員工、主管、HR、系統管理員
  - 三語言完整支援（zh-TW/en/ja）
  - ✓ 完整存取 / △ 部分存取 / — 無存取，顏色視覺化標示

## [0.2.28] - 2026-04-06

### Fixed
- 語言切換 DB update 加 `.catch(() => {})`，Supabase reject 時不阻擋 navigation

## [0.2.27] - 2026-04-06

### Fixed
- Sidebar / BottomNav 語言切換 DB update 改為 `Promise.race` + 2 秒 timeout，避免 Supabase 慢或失敗時卡住

## [0.2.26] - 2026-04-05

### Fixed
- Cookie `secure` flag 改為 `process.env.NODE_ENV === 'production'`，本機開發（HTTP）不再失效
- BottomNav 語言切換 DB update 改為 `await`，確保寫入完成再跳轉

## [0.2.25] - 2026-04-05

### Added
- 新增說明文件頁面 `/help`（登入後可存取，三語言完整內容）
  - 11 個功能模組說明：儀表板、文件、公告、合約、出勤、請假、加班、薪資、專案、回饋、設定
  - 每個模組包含功能說明、主要功能列表、存取權限說明
  - 點擊模組標題可直接跳轉至對應功能
- Sidebar「其他」區塊新增「說明文件」連結
- 手機版 BottomNav「更多」面板新增「說明文件」

### Fixed
- BottomNav 語言切換補上 DB 同步（與 Sidebar 行為一致）
- Cookie 補上 `secure: true`（`/api/locale` 與 `/api/auth/callback`）
- `LANGUAGES` 常數統一定義於 `src/i18n/config.ts`，Sidebar / BottomNav / LoginControls / Quick-Start 全部 import
- Quick-Start 頁面：`LOCALE_COOKIE` 改 import from config，不再 hardcode

## [0.2.24] - 2026-04-05

### Added
- 登入頁右上角新增語言切換 + Dark/Light 主題切換（`LoginControls` client component）
- 新增 Quick Start 指南頁面 `/quick-start`（三語言，zh-TW/en/ja 內容完整）
  - 6 步驟說明：開啟系統 → 登入 → Microsoft 驗證 → MFA 設定 → MFA 驗證 → 完成
  - 頁面內語言切換（無需登入）
  - 推薦驗證器 App 說明（Google/Microsoft Authenticator）
- 登入頁底部新增「Quick Start 指南」連結
- `/quick-start` 加入公開路由（不需登入可存取）

### Fixed
- 英文 projects 頁面：`memberCount`/`totalRecords` 改用 ICU plural 格式（"1 member" 而非 "1 members"）

## [0.2.22] - 2026-04-05

### Fixed
- Contracts 表格欄位標題：`contracts.title`（頁面標題）改用獨立 `contracts.nameColumn` key
- DocumentsClient：DB 動態值 `doc_type`/`folder` 改用 guard check，避免 unknown key 觸發 next-intl 報錯
- DocumentDetailClient：audit log `action` 改用 guard check，不在 catalog 內的值 fallback 回原始字串

## [0.2.21] - 2026-04-05

### Fixed
- Projects 頁面補上 i18n：ProjectsClient、ProjectDetail 全部接上 useTranslations()
- 新增 projects 翻譯 key（41 個），三語同步（721 key 總計）

## [0.2.20] - 2026-04-05

### Added
- 深度 i18n 補完：所有剩餘硬編碼中文字串替換為翻譯 key
  - Contracts（合約類型/狀態篩選/到期警告/操作按鈕）
  - Documents（上傳表單/詳情/分類/狀態）
  - Overtime（申請表單/狀態/計算說明）
  - Leave（申請/審核/類型標籤）
  - Payroll（薪資明細/anomalies/年度報告）
  - Feedback（回饋表單）
  - StatusBadge（通用狀態標籤多語系）
  - Admin（settings/users/leave-types/overtime-rates/attendance-anomalies）
- 三語 JSON 全面同步（686 key，zh-TW/en/ja 完全對齊）

## [0.2.19] - 2026-04-05

### Added
- 全站 i18n 接入：39 個 component 全部接上 useTranslations()/getTranslations()
  - Sidebar、BottomNav、Dashboard、Login、MFA setup/verify
  - Settings（主題/語言/MFA）
  - Attendance（打卡/補打卡/團隊總覽）、Leave calendar
  - Announcements（分類標籤/確認狀態）、Contracts、Documents
  - Overtime、Payroll（含 anomalies）、Projects、Feedback
  - StatusBadge（通用狀態標籤）
  - 所有 Admin 頁面（users/departments/companies/leave-types/leave-balances/overtime-rates/insurance-brackets/bonuses/feedback/audit/attendance/settings）
- 三語 JSON 完全同步（zh-TW/en/ja 共 367 key，0 缺漏）

## [0.2.18] - 2026-04-05

### Fixed
- **語言切換根本修復**：所有 component 接上 `useTranslations()` / `getTranslations()`
  - Sidebar：所有導航標籤、section header、aria-label 使用翻譯 key
  - BottomNav：所有導航標籤、theme label、logout 使用翻譯 key
  - Login page：按鈕文字、說明文字使用翻譯 key
  - Dashboard：歡迎訊息、待辦事項、快速入口使用翻譯 key
- 新增翻譯 key：`nav.more`、`nav.themeLight`、`nav.themeDark`（三語言）

## [0.2.17] - 2026-04-05

### Fixed
- 語言切換改用 GET redirect：`/api/locale?lang=en&redirect=/` → server 設 cookie + 302 回原頁
- 最可靠的 cookie 設定方式：瀏覽器處理 Set-Cookie + redirect，不依賴 fetch 或 document.cookie

## [0.2.16] - 2026-04-05

### Fixed
- 語言切換改用 `document.cookie` 直接設定（跟 EDC 完全一樣），不經 API route，不受 middleware 影響

## [0.2.15] - 2026-04-05

### Fixed
- Sidebar 語言切換改回 `window.location.reload()`（`router.refresh()` 不會重新載入 root layout 的 NextIntlClientProvider）

## [0.2.14] - 2026-04-04

### Fixed
- **語言切換根本原因修復**：`/api/locale` 被 proxy.ts middleware 攔截 → 307 redirect to /login，cookie 從未設上。已加入 publicRoutes bypass。

## [0.2.13] - 2026-04-04

### Fixed
- React hydration error #418：useTheme() SSR/client mismatch，加 mounted guard

## [0.2.12] - 2026-04-04

### Fixed
- 語言切換 API 簡化：`/api/locale` 只設 cookie（不動 DB、不需 auth），完全對齊 mycrm
- DB 語言儲存改為 fire-and-forget，不阻塞 cookie 設定

## [0.2.11] - 2026-04-04

### Fixed
- 語言切換重構：對齊 mycrm 的做法
  - Cookie 名改為 `MYOPS_LOCALE`（避免 generic `locale` 衝突）
  - 移除 `Secure` flag（mycrm/EDC 都沒用，會在 HTTP 環境失效）
  - Sidebar 改用 `router.refresh()` 取代 `window.location.reload()`
  - 新增 `src/i18n/config.ts` 統一管理 cookie 名、支援語言、預設值

## [0.2.10] - 2026-04-04

### Fixed
- 語言切換改用 server-side API route（`/api/locale`）設定 cookie，取代不可靠的 `document.cookie`
- 所有語言切換入口統一走同一個 API：Sidebar、BottomNav、Settings、LocaleSync

## [0.2.9] - 2026-04-04

### Added
- Desktop Sidebar: 使用者資訊旁新增登出按鈕（hover 變紅），collapsed 狀態也有 icon
- Mobile BottomNav「更多」面板底部新增「登出」按鈕（紅色文字）

## [0.2.7] - 2026-04-04

### Fixed
- 系統設定頁：key 名稱對齊 DB seed data，加入 catch-all 群組，移除不存在的 description 欄位
- 語言切換：auth callback 登入時同步 locale cookie，避免多餘的 reload
- 所有 locale cookie 統一加 Secure flag（HTTPS 環境）

## [0.2.6] - 2026-04-04

### Fixed
- Leave calendar 月份切換：API 新增 `start/end/calendar` 參數支援，修正切月後資料錯誤
- 公告語言解析：`resolveContent` 正確處理 `zh-TW` → `zh` 對應
- Daily digest 公告計數：加入 `requires_confirmation=true` + `confirmed_at IS NULL` 過濾

## [0.2.5] - 2026-04-04

### Fixed
- 網頁版語言切換：改用 `useLocale()` 判斷當前語言，修正高亮不同步問題

### Added
- 手機版「更多」面板新增 Dark/Light mode 切換按鈕
- 手機版「更多」面板新增語言切換（中文/EN/日本語）

## [0.1.0-alpha.1] - 2026-04-03

### Added
- Initial Next.js 14 project setup (TypeScript, Tailwind CSS, App Router)
- shadcn/ui component library initialized
- Core packages: Supabase SSR, next-intl, next-themes, react-hook-form, zod, sonner, lucide-react
- Supabase client (server + browser)
- Middleware with AAL MFA enforcement
- i18n setup (zh-TW / en / ja)
- Type definitions and hasFeature utility
